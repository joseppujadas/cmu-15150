structure Twig : TWIG =
struct
  (* IMPLEMENT ME! *)
  type 'a twig = unit

  val empty = raise Fail "Unimplemented"

  val combine = raise Fail "Unimplemented"

  val height = raise Fail "Unimplemented"
end
